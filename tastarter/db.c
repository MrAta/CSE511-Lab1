//
// Created by sps5394 on 10/18/18.
//

#include "db.h"
int db_get(char *key, char **ret_buf, int *ret_len) {
  // Template for Quinn in case this hasn't been implemented, else I can implement
//  while (getline(&db_entry, (size_t *) &line_size, file) != -1) {
//    if (strcmp(strtok(db_entry, " "), key) == 0) {
//      // Found
//      cache_put()
//    }
//  }
  return 0;
}